# Docker

Docker assets will be added after the backend and AI engine can run locally.

Expected files:

- Backend Dockerfile.
- AI engine Dockerfile.
- Local compose file for development.
- App Runner-compatible production container definitions.
